wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  ap:
    ap_timeout: "${sub_ap_timeout}"
    ssid: "${device_name}-setup"
    password: !secret wifi_password

web_server:
  port: 80
  version: 3
  
captive_portal:
