# This file contains all the subsitutions that will be used throughout the yaml files for this device.
# Do not edit this file, this is the default substitutions, edit the head .yaml file for this device.
# Example: my_device.yaml

substitutions:
  # api_key: Place the API key for connecting the device to Home Assistant here.
  # API keys are 32-byte base64-encoded string.
  # To create an API Key (using linux command line):
  #   bash: openssl rand -base64 32
  # Copy the output and place between the quotes.
  # Home Assistant will as for this code when setting up the devices, so put it in another document to copy and paste into Home Assistant.
  api_key: ""
