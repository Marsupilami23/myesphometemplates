# This file is used for diagnostic services

button:
  - platform: restart
    id: restart_button
    name: "${sub_device_name} Restart Firmware"
    entity_category: diagnostic
    disabled_by_default: true
